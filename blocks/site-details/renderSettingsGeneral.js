export default async function renderSettingsGeneral({ container, nav, renderOptions }) {
  const {
    siteDetails, token, siteSlug, user, 
} = renderOptions;

  container.innerHTML = '<img src="/icons/loading.svg" alt="loading" loading="lazy"/>';

  const [

  ] = await Promise.all([

  ]);
}
